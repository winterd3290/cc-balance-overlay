# CONTEXT

- 当前任务：增加全屏看电影和玩游戏时自动隐藏余额显示。
- 本次完成：检测当前前台窗口是否覆盖所在显示器；进入全屏时隐藏余额窗口、悬浮提示和设置面板；退出全屏后自动恢复余额窗口。
- 验证结果：`cargo test` 18 个通过、0 个失败；`cargo build --release` 成功。
- 关键决定：这不是把窗口变成真正系统托盘图标，而是采用接近托盘行为的显示层隐藏机制；不改余额查询、配置格式和用户操作。

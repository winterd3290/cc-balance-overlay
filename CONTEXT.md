# CONTEXT

- 当前任务：等待用户确认等高行设置面板截图。
- 本次完成：余额窗口保持当前修复；设置面板改成 6 行等高，每行 40px；字号、颜色、Claude 前缀、Codex 前缀、开机启动、退出行高统一；右侧控件统一从同一列开始；开机启动勾选框移到右列并显示蓝底白勾。
- 验证结果：`cargo test` 15 个通过、0 个失败；`cargo build --release` 成功；已用新 release exe 运行并截图验证设置页和悬浮提示。
- 关键决定：设置面板不再依赖系统静态标签，避免中文被控件裁切；字号不再使用上下箭头控件，避免微调控件截图和 DPI 下挤压；不改余额查询、配置格式和公开操作。
